This small python script allow me to to enable github pages and set a custom domain.
It also add a reccord on cloudflare to link with github pages.

Basic usage : `python main.py add <sub-domain> <repo-name>`
